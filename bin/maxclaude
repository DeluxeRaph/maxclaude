#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "$0")" && pwd)"
export MAXAGENT_DEFAULT_PROVIDER=claude
export MAXAGENT_SESSION_PREFIX=legacy
export MAXAGENT_APP=maxclaude
exec "$DIR/maxagent" "$@"
