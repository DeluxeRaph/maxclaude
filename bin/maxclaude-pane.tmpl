#!/usr/bin/env bash
# maxclaude-pane — the command each zellij pane runs.
#
# GENERATED by the maxclaude installer. To change the working directory or
# whether Claude starts with --dangerously-skip-permissions, re-run install.sh
# (it rewrites this file) or just edit the two values below.
__ENV_LINES__
cd "__WORKDIR__" 2>/dev/null || cd "$HOME" || true
exec claude __CLAUDE_FLAGS__ "$@"
